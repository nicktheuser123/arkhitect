# Database (required for Arkhitect app)
DATABASE_URL=postgresql://user:password@localhost:5432/arkhitect

# Bubble API, LLM, and Judge0 are configured in the Setup tab (stored in DB)
# Optional: Judge0 (for code execution - public CE has rate limits)
# JUDGE0_API_KEY=your-judge0-api-key
# JUDGE0_BASE_URL=https://ce.judge0.com
